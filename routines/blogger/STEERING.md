# Blogger routine — STEERING (read FIRST every run, append dated bullets LAST)

Standing rules learned from Hassan's live judging of past runs.

- [2026-06-29] On a content REFRESH, Codex flags that land on ALREADY-PUBLISHED claims (pricing, PDPL/positioning) are NOT ship-blockers — surface them clearly for Hassan and ship; don't re-litigate copy that is already live. (He chose "ship as-is" on PR #112 with the pricing/PDPL flags noted.) Still DO fix net-new over-claims the refresh itself introduces (e.g. promoting a roadmap feature to "shipped").
- [2026-06-29] Pricing copy = Type-1 surface: never rewrite a price number unilaterally, even to "fix" a Codex flag. Flag it and let Hassan set the correct figure.
- [2026-06-29] When the task is "write/create a post," RUN THE /KC-blogger SKILL — do not hand-roll a draft inline. Hassan corrected a hand-written refresh with "create it, I meant the skill." The skill enforces the PLAYBOOK gates (SERP-steal, intent, brief-consumption) that ad-hoc writing skips.
- [2026-06-29] Net-new posts: GENERATE the hero + OG images (gen-image.py) BEFORE presenting, per routine step 3b — Hassan expects them, not image-less drafts. Repo reality (2026-06-29): the blog system has NO per-post OG-meta wiring (publish-draft.mjs drops image frontmatter; the [slug] route sets no openGraph.images). The ONLY image path that renders is embedding in the post BODY from app/public/blog/<slug>/. Wiring real <meta og:image> is a separate small website PR — offer it, don't fake inert frontmatter.
- [2026-06-29] Law 2 reminder banked: اودو (8,100/mo) is NAVIGATIONAL — skip it, don't chase the big Odoo-Arabic volume for a blog. The winnable AR informational demand is the محضر/نموذج/تدقيق family.
- [2026-07-08] Media generation = Google Gemini only, already locked in (gen-image.py -> generativelanguage.googleapis.com, gemini-3.1-flash-image-preview). Confirmed with Hassan, don't switch providers.
- [2026-07-08] Any "copy-paste template" promised in a post must ship as a REAL downloadable file, not inline prose/code-fence. Pattern: generate via `scripts/gen-template-docx.py` (python-docx) -> `app/public/downloads/<slug>-template.docx`, wire through frontmatter `template_download_href` + `template_download_label`, rendered by `app/components/blog/template-download.tsx` (tracked: posthog.capture('template_download',...) + gtag). Found because the old hand-rolled markdown renderer (`app/lib/blog.ts`) had NO support for ``` fenced code blocks or GFM tables -- any post using either silently mangled into unreadable run-on prose. Confirmed LIVE-BROKEN on namuthaj-mahdar-ijtimaa-ar's template section (the site's only winning post) on 2026-07-08. Renderer now fixed to support both properly (fix PR, separate from content PRs) -- but templates still go the downloadable-file route per Hassan's instruction, inline fences are for real code only.
- [2026-07-12] New data source: `claude-knowcap/marketing/digital-employees/seo/state/rank-tracker/seo-conversions-pages-daily.csv` (PostHog, first-touch attribution) now tracks organic pageviews + actual beta-signup conversions PER PAGE, not just clicks. As of 2026-07-12: 1 total site-wide conversion (attributed to `/compare/knowcap-vs-otter`, not any blog post) — too little volume to weight topic selection on yet. Once volume grows, `seo_winners` in `opportunity-queue.json` should prefer conversion-linked topics over click-only winners (a post that clicks well but never converts is a weaker signal than one that does) — check that CSV before assuming the current click-based `seo_winners` entry is still the best signal.
