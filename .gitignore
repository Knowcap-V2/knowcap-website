.env
.env.local
.env.*
!.env.example
app/.env
app/.env.local
app/.env.*
!app/.env.example
**/node_modules
.next
.cache
.build
*.nfs*
**/.yarn/
!**/.yarn/patches
!**/.yarn/plugins
!**/.yarn/releases
!**/.yarn/sdks
!**/.yarn/versions
!**/.yarn/cache
**/.yarn/build-state.yml
**/.yarn/install-state.gz
dist
target
out
.deploy
.logs
*.tsbuildinfo
.gstack/

# Routine run logs (per-routine state, not tracked)
routines/**/runs/
routines/**/baselines/
routines/**/state.json
routines/**/opportunity-queue.json
# burn-state.json promoted to tracked 2026-07-16 (growth-manager R5) — same
# rationale as post-outcomes.json (PR #130): an agent-hand-edited counter with
# no git diff is how posts_this_week drifted silently in the first place.

# Database exports / PII dumps — never commit (5804)
database_export/
*.sql
*_dump.json
all_data_combined.json

# Playwright browser-automation debug logs (console + page snapshots)
.playwright-cli/
.playwright-mcp/
.vercel
.env*
